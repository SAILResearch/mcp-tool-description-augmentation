"""Test suite package for MCP-Universe.

This module exists so that helper utilities in the ``tests`` tree can be
imported using standard package syntax (e.g. ``tests.benchmark.cli_support``)
when the benchmark scripts are executed directly.
"""

